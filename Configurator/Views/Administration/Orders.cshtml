@model Configurator.ViewModels.OrdersViewModel

@{
    ViewBag.Title = "Orders";
}

<h2>Orders</h2>

@foreach (var o in Model.AllOrders)
{
    <div>
        <span>@o.ProductCode -  @o.DeliverDate</span>
    </div>
}